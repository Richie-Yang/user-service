export enum NodeEnv {
  LOCAL = 'local',
  DEV = 'dev',
  PROD = 'prod',
}

export enum Models {
  USER = 'user',
  TODO = 'todo',
  TODO_LIST = 'todoList',
}

export enum Roles {
  ADMIN = 'admin',
  USER = 'user',
}

export enum Status {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}
